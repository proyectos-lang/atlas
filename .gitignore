# Dependencias y compilación
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Secretos: NUNCA subir. Contienen la service role key de Supabase
# y la clave de Anthropic.
.env
.env.local
.env*.local

# Datos de estudiantes. El repositorio es público: el archivo de origen
# se comparte por fuera, no aquí. Ver README > Puesta en marcha.
datos/*.xlsx
datos/*.xls
datos/*.csv

# Capturas y scripts de verificación puntual.
# OJO: no usar *.mjs — excluiría next.config.mjs y postcss.config.mjs,
# y sin este último Tailwind no compila y el sitio sale sin estilos.
*.png
prueba-*.ts
verif-*.mjs
diag-*.mjs

# Sistema y cobertura
.DS_Store
coverage/
